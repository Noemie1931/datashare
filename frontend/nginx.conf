server {
  listen 80;

  location / {
    root /usr/share/nginx/html;
    index index.html;
    # SPA : toutes les routes inconnues (ex. /d/xxx, /login) sont renvoyees
    # vers index.html, c'est React Router qui gere la navigation cote client.
    try_files $uri $uri/ /index.html;
  }
}
